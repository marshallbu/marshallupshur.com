/*global define: false */

(function (_, app) {
    'use strict';

    var NavCtrl = ['$scope', '$log',
        function ($scope, $log) {




            // $scope.$apply();
        }
    ];

    module.exports = NavCtrl;

    return NavCtrl;
})(
    require('lodash'),
    require('../app')
);